# Measured corpus v0

Contract for a **future** three-arm runner. Brand: Agent Control Lab. Apache-2.0.

This directory is a row schema plus a seed map. It does **not** run fixtures and it is **not** a measured attack-success scoreboard. **No ASR.**

- Schema: [`schema/row.schema.json`](schema/row.schema.json) (notes in [`schema/README.md`](schema/README.md))
- Seed map: [`index.json`](index.json) — pointers at existing `eval/joint_story/` fixtures
- Narrative: [`docs/measured-corpus-v0.md`](../../docs/measured-corpus-v0.md)

Public/EOI claim cite stays pep diligence tip `1d0f380`. This seed does not lift that cite.
